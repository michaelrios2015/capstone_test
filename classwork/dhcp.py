# so we are making a fake dhcp. this will have 10 ip adresses we want to be able to 
# 1) assign an ip adress
# 2) release an IP adress
# 3) list all avaible adresses
# 4) list all unaviable adresses 

# this should be done in object oriented programing 

ip = [0, 1, 2, 3]

avaible = [0, 0, 0, 0]

class dhcp:

    def __init__():
        
        ipAdress = []


    def init_array():
        
        # add 9 more
        ipAdress.append("192.168.7.10")


    def listAll():
        for ip in ipAdress